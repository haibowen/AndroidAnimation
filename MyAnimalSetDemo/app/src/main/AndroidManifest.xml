<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ekwing.myanimalsetdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ToolActivity"></activity>
        <activity android:name=".activity.FromActivity" />
        <activity android:name=".activity.TranslateActivity" />
        <activity android:name=".base.BaseActivity" />
        <activity
            android:name=".activity.ExposeActivity"
            android:theme="@style/ExposeTheam" />
        <activity android:name=".activity.AttributeActivity" />
        <activity android:name=".activity.ViewActivity" />
        <activity android:name=".activity.FrameActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>